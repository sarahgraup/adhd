/**
 * POST /reminders/{reminderId}/settings - Create settings for a specific reminder.
GET /reminders/{reminderId}/settings - Retrieve settings for a specific reminder.
PUT /reminders/{reminderId}/settings - Update settings for a specific reminder.
DELETE /reminders/{reminderId}/settings - Delete settings for a specific reminder.
 */